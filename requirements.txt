# N통합 발행기 — Python 3.12 의존성
#
# ⚠ 플랫폼: Windows 전용.
#   PowerShell(테더링/IP 변경), ctypes.windll(콘솔 숨김·캡스락), pythonw 런처,
#   C:\ 경로 등 Windows API에 의존합니다. macOS/Linux에서는 동작하지 않습니다.
PySide6>=6.6.0
playwright>=1.40.0
anthropic>=0.40.0
openai>=1.30.0
Pillow>=10.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
imageio-ffmpeg>=0.4.9
kiwipiepy>=0.17.0
qt-material>=2.14
